"use strict";
/**
 * Check if the current browser supports the web-audio-api .
 */
var win = window;
win.AudioContext = win.AudioContext ||
    win.webkitAudioContext ||
    win.mozAudioContext ||
    win.msAudioContext;
exports.HasAudioContext = !!win.AudioContext;
